{
  "devDependencies": {
    "vitepress": "2.0.0-alpha.16"
  },
  "scripts": {
    "docs:dev": "vitepress dev --host",
    "docs:build": "vitepress build && node scripts/generate-sitemap.mjs",
    "docs:preview": "vitepress preview"
  },
  "dependencies": {
    "markdown-it-custom-attrs": "^1.0.2"
  }
}